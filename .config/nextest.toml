[store]
dir = "target/nextest"

# Hardware-in-the-loop only (`hil` feature + `#[ignore]`; run with
# `--features hil --profile hil -- --ignored`).
[profile.hil]
default-filter = 'test(hil_loopback_sketch)'
