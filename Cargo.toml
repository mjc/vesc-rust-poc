[workspace]
members = [
    "crates/vesc-ffi",
    "crates/vesc-units",
    "crates/vesc-sdk",
    "crates/vesc-protocol",
    "crates/vesc-pkg",
    "crates/vesc-cli",
    "examples/loopback",
]
default-members = [
    "crates/vesc-cli",
    "examples/loopback",
]
resolver = "2"

[workspace.lints.rust]
missing_docs = "warn"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"

[profile.dev]
panic = "abort"
