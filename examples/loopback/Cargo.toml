[package]
name = "vesc-example-loopback"
version = "0.1.0"
edition = "2024"

publish = false

[lints]
workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]
vescpkg = { path = "../../crates/vescpkg" }

[features]
test-support = ["vescpkg/test-support"]
