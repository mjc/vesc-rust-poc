[package]
name = "vesc-example-loopback"
version = "0.1.0"
edition = "2024"

publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
vesc-sdk = { path = "../../crates/vesc-sdk" }

[features]
test-support = ["vesc-sdk/test-support"]
