[package]
name = "vesc-protocol"
version = "0.1.0"
edition = "2024"
description = "Unofficial shared Rust protocol types for VESC-related host and package experiments."

[lints]
workspace = true

[dependencies]
no-panic = "0.1.36"
