[package]
name = "vesc-package"
version = "0.1.0"
edition = "2024"

[dependencies]
vesc-ffi = { path = "../vesc-ffi" }
vesc-protocol = { path = "../vesc-protocol" }

[features]
test-support = []

[dev-dependencies]
rstest = "0.26"
