[package]
name = "vesc-float-out-boy-protocol"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "Float Out Boy-specific no_std host/device protocol types and encoders."

[lints]
workspace = true

[features]
test-support = []

[dependencies]
bitflags = { version = "2.13.1", default-features = false }
vesc-protocol = { path = "../vesc-protocol" }
vescpkg-rs = { path = "../vescpkg-rs" }
