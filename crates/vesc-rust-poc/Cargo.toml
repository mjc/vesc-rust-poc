[package]
name = "vesc-rust-poc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
vesc-ffi = { path = "../vesc-ffi" }
vesc-protocol = { path = "../vesc-protocol" }
