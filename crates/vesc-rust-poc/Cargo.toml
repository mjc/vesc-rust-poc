[package]
name = "vesc-rust-poc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
vesc-package = { path = "../vesc-package" }

[features]
test-support = ["vesc-package/test-support"]
