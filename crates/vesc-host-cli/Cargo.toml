[package]
name = "vesc-host-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
vesc-protocol = { path = "../vesc-protocol" }

[dev-dependencies]
vesc-pkg-build = { path = "../vesc-pkg-build" }
